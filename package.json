{
  "name": "baishen.me",
  "version": "1.0.0",
  "main": "sass/style.sass",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "bulma": "^0.7.5",
    "node-sass": "^4.12.0"
  },
  "scripts": {
    "css-build": "node-sass --omit-source-map-url --output-style=compressed static/sass/style.sass static/css/style.css",
    "css-watch": "npm run css-build -- --watch",
    "start": "npm run css-watch"
  }
}
